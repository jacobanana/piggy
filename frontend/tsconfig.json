{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": false,
    "skipLibCheck": true,
    "isolatedModules": true,
    "types": ["vite/client"]
  },
  "include": ["src"],
  "//": "The worker is checked by tsconfig.worker.json against the WebWorker lib; leaving it here too would check it against the DOM, where `self` is a Window and nothing in sw.ts means what it says.",
  "exclude": ["src/sw.ts"]
}
